<%= form_with url: reject_comment2_path(@comment2), method: :post, local: true do |form| %>
  <div class="mb-3">
    <label for="rejection_feedback" class="form-label">Rejection Feedback</label>
    <%= form.text_area :rejection_feedback, 
        class: "form-control", 
        id: "rejection_feedback", 
        rows: 6, 
        placeholder: "Please provide detailed feedback on why this comment2 is being rejected...",
        required: true %>
    <div class="form-text">This feedback will be visible to the comment2 author and help them understand the rejection.</div>
  </div>
  <div class="d-flex justify-content-between">
    <% title, button_classes = model_link("Comment2", :reject, current_user) %>
    <%= form.submit title, class: button_classes %>
  </div>
<% end %>
